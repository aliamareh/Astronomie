<form method="post" action="{{ path('app_messier_catalogue_delete', {'id': messier_catalogue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messier_catalogue.id) }}">
    <button class="btn">Delete</button>
</form>
